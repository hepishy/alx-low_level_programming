#include <stdio.h>

/**
 * main - Entry point
 * Description: print alphabets
 * Return: Always Return 0
*/
int main(void)
{
	int n = 0;

	while (n < 10)
	{
		printf(n);
		n++;
	}
	putchar('\n');
	return (0);
}
