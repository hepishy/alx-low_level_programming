#include <stdlib.h>
#include "main.h"

/**
 * _abs - return the absolute value of n
 * @n : the input to check
 * Return: Retrun 1 if + , -1 if - ,0 if equal 0.
 */
int _abs(int n)
{
	return (abs(n));
}
